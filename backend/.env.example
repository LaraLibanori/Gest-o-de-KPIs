# Supabase > Project Settings > API
SUPABASE_URL=https://xxxxxxxx.supabase.co

# Supabase > Project Settings > Database > Connection string > Transaction pooler (porta 6543)
DATABASE_URL=postgresql://postgres.xxxxxxxx:SENHA@aws-0-sa-east-1.pooler.supabase.com:6543/postgres

# Só para projeto antigo, que assina o JWT com segredo compartilhado (HS256).
# Projeto novo usa chave assimétrica e deixa isso vazio.
SUPABASE_JWT_SECRET=
